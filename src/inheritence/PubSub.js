class PubSub {
  subscribers = {
    any: []
  };

  subscribe(fn, type = "any") {
    
  }

  unsubscribe(fn, type) {
    
  }

  publish(publication, type = "any") {
    
  }
}

export default PubSub;
